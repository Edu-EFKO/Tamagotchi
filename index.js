// Инициализация базовых данных питомца
let petName = 
// Инициализируйте переменные для состояния питомца: счастье, энергия, голод

alert("Поздравляю, ваш питомец " + petName + " готов к игре!");

// Вывод начального состояния питомца в консоль
console.log(`%cНачальное состояние питомца:`, "color: blue; font-weight: bold;");
// Выведите состояние питомца: счастье, энергия, голод

// Функция для кормления питомца
function feedPet() {
    // Добавьте логику кормления питомца (уменьшение голода, увеличение энергии)
    console.info(`%c${petName} поел.`, "color: green; font-style: italic;");
    // Вывод состояния голода и энергии питомца в консоль
}

// Функция для игры с питомцем
function playWithPet() {
    // Добавьте логику для увеличения счастья и уменьшения энергии после игры
    console.info(`%c${petName} поиграл.`, "color: green; font-style: italic;");
    // После игры питомец становится голоднее
    // Вывод состояния счастья, энергии и голода в консоль
}

// Функция для проверки состояния питомца
function checkPet() {
    // Вывод текущего состояния питомца
}

// Основной цикл игры
let gameActive = true; // Переменная для управления циклом игры
while (gameActive) {
    let action = prompt("Что вы хотите сделать с вашим питомцем? (feed/play/check)");

    if (action === "feed") {
        feedPet();
    } else if (action === "play") {
        playWithPet();
    } else if (action === "check") {
        checkPet();
    } else {
        console.error("%cНеправильный выбор. Попробуйте снова.", "color: red; font-weight: bold;");
        alert("Неправильный выбор. Попробуйте снова.");
        continue; // Пропуск итерации при неверном выборе
    }

    // Проверка, если голод достигает максимума
    if (hunger >= 100) {
        // Выводить причину остановки игры
        gameActive = false;
    }
    
    // Проверка, если энергия становится равной или меньше 0
    if (energy <= 0) {
        // Выводить причину остановки игры
        gameActive = false;
    }

    // Проверка, если питомец достигает максимального счастья, полной энергии и не голоден
    if (happiness === 100 && energy === 100 && hunger === 0) {
     // Выводить уведомление
    }
}

// Вывод итогового сообщения о завершении игры
console.log(`%cИгра закончилась. Финальное состояние питомца:`, "color: blue; font-weight: bold;");
// Выведите финальное состояние питомца: счастье, энергия, голод
alert("Игра закончилась. Смотрите результат в консоли.");
